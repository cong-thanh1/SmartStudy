{
  "name": "@smartstudy/cloudflare-ai-relay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260716.1",
    "@types/node": "^24.0.0",
    "typescript": "^6.0.2",
    "vitest": "^3.2.4",
    "wrangler": "^4.111.0"
  }
}
